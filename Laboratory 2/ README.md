# Laboratory Activity #2: Working with Analog Signals

## 📜Description

This laboratory activity focuses on implementing analog signal control in an Arduino-based circuit. Building on the previous activity, a running light system is created using LEDs, where brightness is controlled using Pulse Width Modulation (PWM) through the `analogWrite()` function. The activity demonstrates how analog behavior can be simulated using digital outputs.


## 🖇️Objectives

* Discuss analog signals and their implementation in an Arduino circuit
* Understand analog-to-digital signal conversion using the `map()` function


## ⚓Concepts

* Analog signals and PWM (Pulse Width Modulation)
* Analog-to-digital signal mapping using `map()`
* LED brightness control using `analogWrite()`
* Use of arrays for pin management
* Program flow control using `while()` loops


## ⚙️System Overview

The system consists of an Arduino microcontroller connected to five LEDs assigned to digital pins 8 through 12. The LEDs are controlled in a running light sequence from pin 12 down to pin 8. Using an array, the pins are initialized and controlled within a `while()` loop. The brightness of each LED is adjusted using PWM signals generated by the Arduino, allowing analog-like behavior with a fixed one-second delay between transitions.

## 💡Conclusion

This activity demonstrates how Arduino can simulate analog output using digital pins through PWM. By combining arrays, loops, and the map() function, students gain a deeper understanding of analog signal control and brightness modulation, which are essential concepts for sensor-based and IoT applications.

## 🗂 Files Included
| File Name | Description |
|----------|-------------|
| `lab2_activity.ino` | Arduino sketch for PWM-based LED control using arrays and while() loops |
| `lab2_breadboard_diagram.png` | Breadboard diagram illustrating the LED wiring and pin connections for the analog signal activity |

